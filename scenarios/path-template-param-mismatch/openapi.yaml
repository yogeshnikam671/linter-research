openapi: 3.0.3
info:
  title: Path Template Parameter Mismatch
  version: 1.0.0
paths:
  /users/{userId}:
    get:
      operationId: getUserMismatchedPathParam
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
