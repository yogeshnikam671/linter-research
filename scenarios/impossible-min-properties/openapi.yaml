openapi: 3.0.3
info:
  title: Impossible minProperties
  version: 1.0.0
paths:
  /configs:
    post:
      operationId: createConfig
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 3
              additionalProperties: false
              properties:
                name:
                  type: string
                mode:
                  type: string
      responses:
        "201":
          description: Created
