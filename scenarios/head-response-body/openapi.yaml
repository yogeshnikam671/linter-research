openapi: 3.0.3
info:
  title: HEAD With Body
  version: 1.0.0
paths:
  /users:
    head:
      operationId: headUsersBody
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
